#!/bin/sh
is_installed=$(dpkg-query -W -f='${Status}' openrazer-meta 2>/dev/null | grep -c "ok installed")

if [ $is_installed -eq 0 ]
then
	sudo gpasswd -a $USER plugdev    
else
	sudo gpasswd -d $USER plugdev && sudo apt autoremove
fi
